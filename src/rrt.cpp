#include "rrt.hpp"

////////////////////////////////////////////////////////////////////////////////
RRT::RRT(Point _start, Point _end)
: iterations(500), threshold(0.5), sampleRange(10), maxStep(2.0)
{
  start = _start;
  end = _end;
}

////////////////////////////////////////////////////////////////////////////////

